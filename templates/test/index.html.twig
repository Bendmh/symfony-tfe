{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}

<div class="example-wrapper">

    {{ form_start(form) }}

    {{ form_widget(form.name) }}
    {{ form_widget(form.Lesson_category) }}

    <button type="submit">envoyer</button>
    {{ form_end(form) }}
</div>
{% endblock %}
